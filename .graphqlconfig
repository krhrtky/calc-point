{
  "name": "GitHub GraphQL Schema",
  "schemaPath": "schema.graphql",
  "extensions": {
    "endpoints": {
      "Default GraphQL Endpoint": {
        "url": "https://api.github.com/graphql",
        "headers": {
          "user-agent": "JS GraphQL",
          "Authorization": "bearer ${env:ORG_GRADLE_PROJECT_githubToken}"
        },
        "introspect": false
      }
    }
  }
}
